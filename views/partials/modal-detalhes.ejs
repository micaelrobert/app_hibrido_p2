<style>
  .modal-overlay {
    display: none;
    position: fixed;
    z-index: 9999;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.85);
    backdrop-filter: blur(2px);
    align-items: center;
    justify-content: center;
  }

  .modal-window {
    background-color: #0a0a0a;
    border: 2px solid #00ff00;
    box-shadow: 0 0 20px rgba(0, 255, 0, 0.4);
    width: 90%;
    max-width: 600px;
    max-height: 80vh;
    display: flex;
    flex-direction: column;
    font-family: 'Courier New', Courier, monospace;
  }

  .modal-header {
    background: #003300;
    padding: 10px 15px;
    border-bottom: 1px solid #00ff00;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .modal-title { color: #00ff00; font-weight: bold; font-size: 1.2em; }
  .modal-close {
    color: #00ff00; background: transparent; border: 1px solid #00ff00;
    font-weight: bold; cursor: pointer; padding: 2px 8px;
  }
  .modal-close:hover { background: #00ff00; color: #000; }

  .modal-body { padding: 15px; overflow-y: auto; color: #ccc; }
  .modal-item { border-bottom: 1px dashed #333; padding: 8px 0; display: flex; justify-content: space-between; }
  .modal-item:last-child { border-bottom: none; }
  .item-date { font-size: 0.8em; color: #666; }
</style>

<div id="modal-detalhes" class="modal-overlay">
  <div class="modal-window">
    <div class="modal-header">
      <span id="modal-titulo" class="modal-title">>> DETALHES</span>
      <button class="modal-close" onclick="fecharModal()">[X]</button>
    </div>
    <div id="modal-lista" class="modal-body">
      <p class="console-text">Carregando...</p>
    </div>
  </div>
</div>